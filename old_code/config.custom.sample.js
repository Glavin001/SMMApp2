/**
Custom Configuration
This will take in the argument `config` and manipulate it's configuration values.
*/
module.exports = function(config) {

    // Server configuration
    /*
    config.server.port = 8081; // Default is 8080
    */

    // Twilio
    /*
    config.twilio.sid = "ACCOUNT_SID";
    config.twilio.token = "AUTH_TOKEN";
    config.twilio.number = "+14506667788"; // A number you bought from Twilio and can use for outbound communication
    */
    
};